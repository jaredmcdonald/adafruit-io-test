{
  "name": "adafruit-io-test",
  "description": "client to blink a LED on an RPi",
  "version": "0.0.1",
  "dependencies": {
    "adafruit-io": "^4.2.7"
  },
  "devDependencies": {},
  "scripts": {
    "configure": "./configure.bash",
    "start": "./start.bash",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC"
}
